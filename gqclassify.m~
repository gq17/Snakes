% classify automatically
clear all, close all;

% sample
norm   = [];
abnorm = [];

% get the contour sequence from the heart video and save them to file
ext = '.avi';
for i=1:size(norm,2)
    n        = norm(i);
    filename = strcat('../examples/abnormal/',n,'.avi');
    contouri = getContour(filename);
    save
end

% load data
load('data.mat');
